// 5. На входе статичный объект. Необходимо сформировать массив из всех нечетных
// ключей объекта.


const obj = {
    id:2
}

const arr = [];
for(const key in obj){
    if (key) {
        
    } else {
        
    }
}